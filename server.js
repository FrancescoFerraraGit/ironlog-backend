"const express = require('express');const app = express();const PORT = process.env.PORT || 5000;app.get('/', (req, res) => {  res.send('Server backend di IronLog in esecuzione!');});app.listen(PORT, () => {  console.log(\`Server in esecuzione sulla porta \${PORT}\`);});" 
